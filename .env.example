# 🌍 Общие настройки
TIMEZONE=Europe/Moscow
STATE_DAYS_LIMIT=3          # Хранить отправленные новости за 3 дня
DAYS_LIMIT=1                # Проверять новости за 2 дня
NEWS_LIMIT=1                # Количество новостей за цикл
INTERVAL=220                # Интервал между циклами (сек)
ROUND_ROBIN_MODE=1          # Циклический обход RSS
PARSE_MODE=HTML             # Форматирование сообщений в Telegram

# 🧠 Модели
AI_MODEL=gemini-2.0-flash
OLLAMA_MODEL=qwen3:4b 
OLLAMA_MODEL_FALLBACK=gpt-oss:20b

GEMINI_MAX_TOKENS=500
OLLAMA_MAX_TOKENS=500
MODEL_MAX_TOKENS=500

ACTIVE_MODEL=gemini-2.0-flash  # какая модель активна (gemini / ollama)

# 🚀 Лимиты парсера и таймауты
PARSER_MAX_TEXT_LENGTH=5000    # макс. длина статьи (символов)
OLLAMA_TIMEOUT=600             # секунд ожидания ollama

# 🤖 Telegram
TELEGRAM_TOKEN=
CHAT_ID=
AI_STUDIO_KEY=
AI_PROJECT_ID=

# если используешь прокси:
# HTTP_PROXY=
# HTTPS_PROXY=

# ---------------- Prompt templates (optional)
# Use {content} as placeholder for article text
GEMINI_PROMPT=Сделай краткое, связное резюме новости на русском языке. Без вступлений. Раздели на 2–3 коротких абзаца по смыслу. Добавь 2-3 уместных эмодзи в тексте (не в начале и не в конце). Часто используй HTML-теги (<b>, <i>, <u>, <s>, <tg-spoiler>, <code>, <pre>, <blockquote>) для Telegram:\n{content}
OLLAMA_PROMPT=Сделай краткое, связное резюме новости на русском языке. Без вступлений. Раздели на 2–3 коротких абзаца по смыслу. Добавь 2-3 уместных эмодзи в тексте (не в начале и не в конце). Часто используй HTML-теги (<b>, <i>, <u>, <s>, <tg-spoiler>, <code>, <pre>, <blockquote>) для Telegram:\n{content}

# 💡 Совет:
# RSS_URLS можно вынести в отдельный файл rss.txt (по строке на каждый URL),
# и в коде просто читать его (чтобы не терять читаемость .env)

# ⚙️ Батчи и паузы
BATCH_SIZE_SMALL=5
PAUSE_SMALL=3
BATCH_SIZE_MEDIUM=15
PAUSE_MEDIUM=5
BATCH_SIZE_LARGE=25
PAUSE_LARGE=10
SINGLE_MESSAGE_PAUSE=1

# Рекомендую PAUSE_SMALL ≥ 2, иначе Telegram может вернуть 429 (rate limit)

# 🔒 SSL
SSL_VERIFY=1

# 📄 Логирование
LOG_FILE=parser.log  # путь к лог-файлу (в корне проекта)

# Дополнительно можно указать уровень логирования:
# LOG_LEVEL=INFO  (или DEBUG)
