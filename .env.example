# 🌍 Общие настройки
TIMEZONE=Europe/Moscow
STATE_DAYS_LIMIT=3
DAYS_LIMIT=2                # проверять за 2 дня — точнее
NEWS_LIMIT=2                # 1 слишком мало, 2 ускорит обновления
INTERVAL=120                # 40 сек → слишком часто, 2 мин лучше
ROUND_ROBIN_MODE=1
PARSE_MODE=HTML

# 🧠 Модели
AI_MODEL=gemini-2.0-flash
OLLAMA_MODEL=qwen3:4b
OLLAMA_MODEL_FALLBACK=gpt-oss:20b

GEMINI_MAX_TOKENS=700       # немного выше — меньше усечений
OLLAMA_MAX_TOKENS=700
MODEL_MAX_TOKENS=700

ACTIVE_MODEL=gemini         # явно указать активную

# 🚀 Лимиты парсера и таймауты
PARSER_MAX_TEXT_LENGTH=8000 # 5000 маловато для длинных статей
OLLAMA_TIMEOUT=120          # 600 избыточно, 120 достаточно
MODEL_TIMEOUT=120            # добавить — общий лимит для всей цепочки

# 🔁 Повторные попытки Gemini
GEMINI_MAX_RETRIES=3         # до трёх попыток при 503
GEMINI_RETRY_DELAY=3         # пауза между попытками (сек)

# 🤖 Telegram
TELEGRAM_TOKEN=
CHAT_ID=
AI_STUDIO_KEY=
AI_PROJECT_ID=

# ---------------- Prompt templates
GEMINI_PROMPT=Сделай краткое, связное резюме новости на русском языке. Без вступлений. Раздели на 2–3 коротких абзаца по смыслу. Добавь 2–3 уместных эмодзи в тексте (не в начале и не в конце). Часто используй HTML-теги (<b>, <i>, <u>, <s>, <tg-spoiler>, <code>, <pre>, <blockquote>) для Telegram:\n{content}
OLLAMA_PROMPT=Сделай краткое, связное резюме новости на русском языке. Без вступлений. Раздели на 2–3 коротких абзаца по смыслу. Добавь 2–3 уместных эмодзи в тексте (не в начале и не в конце). Часто используй HTML-теги (<b>, <i>, <u>, <s>, <tg-spoiler>, <code>, <pre>, <blockquote>) для Telegram:\n{content}

# ⚙️ Батчи и паузы
BATCH_SIZE_SMALL=5
PAUSE_SMALL=2
BATCH_SIZE_MEDIUM=15
PAUSE_MEDIUM=5
BATCH_SIZE_LARGE=25
PAUSE_LARGE=10
SINGLE_MESSAGE_PAUSE=2
SMART_PAUSE=1
SMART_PAUSE_MIN=5
SMART_PAUSE_MAX=15          # чуть больше — безопаснее при API паузах

# 🔒 SSL
SSL_VERIFY=1

# 🌐 aiohttp
AIOHTTP_TOTAL_TIMEOUT=30    # общий запрос может занимать дольше
AIOHTTP_CONNECT_TIMEOUT=10  # 5 → иногда мало
CONNECTOR_LIMIT=50
DNS_TTL_CACHE=300

# 📄 Логирование
LOG_FILE=parser.log
LOG_LEVEL=INFO
